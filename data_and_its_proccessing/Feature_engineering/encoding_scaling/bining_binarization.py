#  descretization or bining:
#  Unsupervised and supervised bining
"""
    Unsupervised:
        -> Equal width bining(Uniform):
            select number of bins,then 
            intervals-> (Max-min)/bins
        -> Equal frequency bining(Quantile bining):
            select number of bins,then
            each bin will contain nums/bins percentage of the observations
            example: if 5 bins in 100 observations:
            each bin will contain 20 observations no matter the values
        -> K-means bining:
            by using kmeans clusttering algorithm,
    Supervised:
        ->
"""

#    Skipped -> should learn again!
